# Railway deployment configuration
# Railway auto-detects PHP + Apache with Nixpacks
# Set MONGO_URI and MONGO_DB in the Railway Variables tab

[build]
builder = "nixpacks"

[deploy]
startCommand = "apache2-foreground"
